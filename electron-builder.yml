appId: com.caiban.island
productName: 采办岛
directories:
  output: release
  buildResources: resources
files:
  - out/**
  - resources/**
  - scripts/caiban-cli.mjs
  - package.json
win:
  target:
    - target: portable
      arch: [x64]
    - target: zip
      arch: [x64]
  icon: resources/icon.png
portable:
  artifactName: Caiban-Island-${version}-Windows-${arch}.${ext}
artifactName: Caiban-Island-${version}-Windows-${arch}.${ext}
electronLanguages:
  - zh-CN
  - en-US
