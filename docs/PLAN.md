# 采办岛 开发计划

## 1. 背景

旧版 DeskManager（WinUI 3 + .NET 10、签名 MSIX、仅 Win11）作为设计与规格参考；新版「采办岛」以 Electron 重写，目标 Win10(1809+)/Win11、免证书绿色分发、灵动岛三级面板、Qoder MCP 智能拆解节点。旧版 docs/（PRODUCT_SPEC、DESIGN_SYSTEM、ARCHITECTURE、TEST_PLAN）的设计资产已移植并扩充到本仓库。

## 2. 决策记录

| 决策点 | 结论 | 理由 |
| --- | --- | --- |
| 技术栈 | Electron + TypeScript + React 19 + Vite + better-sqlite3 | 本机 Node 24 就绪；透明/置顶/磨砂/触控方案成熟；迭代最快 |
| 分发 | 免证书绿色 zip（portable），可选 NSIS | 用户要求无证书；SmartScreen 提示可接受 |
| 界面 | 岛内三级 L1/L2/L3，无独立主窗口 | 用户确认；托盘承接常驻控制 |
| AI 主通道 | Qoder（独立桌面 IDE）MCP：SSE 为主 + STDIO 备用 | Qoder 官方支持 STDIO/SSE 两种 MCP 模式；用户仅能用 Qoder 企业账号 |
| AI 兜底 | 内置 OpenAI-compatible API（未配 Key 时入口隐藏） | 用户选择：Qoder 为主 + 兜底通道 |
| 节点色 | 已完成=绿、进行中=蓝、待完成=黄 | 沿用旧版配色 |
| 折叠形态 | 96×6px 仅露约 4px 提示条 | 用户确认不遮挡浏览器标签/窗口按钮 |
| 滑动 | 左右滑动浏览卡片（惯性+吸附） | 用户确认 |
| 杂事 | 轻量任务：一句话+可选截止时间，无节点 | 用户确认 |
| 提醒 | Windows Toast（无需证书）+ 岛内轻弹降级 | 用户确认条件：无证书则用 Toast |
| 存储 | SQLite 主库 + Markdown/JSON 快照归档 | 查询可靠 + 快照可读可迁移 |
| 工程纪律 | 每个节点：测试机制 → 全部通过 → 一次 git commit | 用户要求 |

## 3. 里程碑（节点完成 = 测试通过 + git commit）

| 节点 | 范围 | 测试机制 | 验收标准 | 提交信息 |
| --- | --- | --- | --- | --- |
| P0 | 文档 7 件套 + git init | 文档一致性检查 | 检查 0 错误；SPEC 覆盖 8 条需求 | P0: 规划与文档交付 — 测试与验收通过 |
| P1 | 工程骨架；L1/L2/L3 形变；磨砂+回退；置顶；托盘；点击穿透 | Vitest 冒烟 + Win10/Win11 手工矩阵 | 三态 60fps；悬停不抢焦点 | P1: 窗口壳 — 测试与验收通过 |
| P2 | SQLite；任务 CRUD；排序；卡片；滑动浏览 | 单测：排序/校验/持久化 | 排序单测全绿；滑动跟手 | P2: 数据与卡片 — 测试与验收通过 |
| P3 | 速览详情；时间轴；节点三态；链接；备注 | 单测：状态流转/安全渲染 | 全部通过 | P3: 详情与时间轴 — 测试与验收通过 |
| P4 | Toast+轻弹；归档；快照；归档页；设置 | 集成：快照往返/提醒调度 | 全部通过 | P4: 提醒与归档 — 测试与验收通过 |
| P5 | MCP SSE+STDIO；内置 API；草稿审核 | 集成：mock MCP/LLM；Qoder 联调 | 全工具可用；草稿原则 | P5: AI 通道 — 测试与验收通过 |
| P6 | 动效打磨；无障碍回退；全屏检测；打包 | 打包冒烟 + 真机矩阵 | 矩阵全通过 | P6: 打磨发布 — 测试与验收通过 |

## 4. 风险

| 风险 | 影响 | 缓解 |
| --- | --- | --- |
| Win10 老显卡磨砂失败 | 视觉降级 | 纯色 #111216 回退 |
| 企业网络/杀软拦本地端口 | MCP SSE 不可用 | STDIO shim；端口占用自动更换 |
| Qoder 版本差异 | 注册步骤不同 | README 提供双传输说明 |
| Electron 内存偏高 | 常驻成本 | 可接受；性能目标 <250MB |
| SmartScreen 提示 | 用户疑虑 | README 说明非报毒 |

## 5. 遗留问题 / 后续增强

- 自定义排序（长按拖拽重排）；
- Arm64 安装包；
- 多显示器多岛；
- 节点依赖关系与子节点；
- 附件托管（复制文件本体）；
- 数据加密与导出密码保护。
