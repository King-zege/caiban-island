# 采办岛 v1 设计系统

## 1. 设计方向

借鉴 Apple Live Activities / Dynamic Island 的信息层级与连续动效，而非复制 iPhone 外形。遵循：

1. **一眼可读**：L1 只表达入口，L2 只展示活跃任务状态，复杂编辑进入 L3。
2. **连续变化**：同一内容在 L1/L2/L3 之间保持空间关系，避免突然换页。
3. **材质克制**：仅 L2/L3 外壳使用系统 Acrylic；内部主要用留白、分组与细分隔建立层级，禁止卡片套卡片。
4. **不打断工作**：悬停不抢焦点，自动收起可预测，全屏场景主动退让。
5. **信息不依赖颜色**：图标、文字、形状与颜色共同表达状态。

参考：Apple Materials 与 Live Activities 设计指南；Windows 桌面 Acrylic（Win10 1803+/Win11 经 SetWindowCompositionAttribute 实现）。

## 2. 设计 Token

### 2.1 颜色（跟随系统；高对比度模式改用系统颜色）

| Token | 深色值 | 浅色值 | 用途 |
| --- | --- | --- | --- |
| Color.Surface.IslandTint | #0A0C0F | #F6F7FA | L2/L3 外壳 Acrylic 偏色 |
| Color.Surface.Fallback | #111318 | #F2F4F7 | 透明关闭、减少动画或磨砂不可用 |
| Color.Surface.Raised | #FFFFFF 6% | #FFFFFF 72% | 有真实层级的内容面 |
| Color.Surface.Hover | #FFFFFF 10% | #111318 5% | hover / 键盘焦点背景 |
| Color.Surface.Pressed | #FFFFFF 14% | #111318 8% | pressed / 拖动中 |
| Color.Border.Subtle | #FFFFFF 10% | #111318 10% | 分组与控件描边 |
| Color.Text.Primary | #F5F7FA | #15171A | 主标题、关键数值 |
| Color.Text.Secondary | #B8BDC7 | #5B616B | 描述、时间、辅助信息 |
| Color.Text.Tertiary | #858B96 | #656C76 | 弱提示；不得用于关键状态；在确定性 Raised/Fallback 背景上满足 4.5:1 |
| Color.Accent / Focus | #64D2FF | #0078D4 | 唯一交互强调色与 2px 焦点环 |
| Color.Node.Completed | #32D74B | #16843A | 已完成，配对 Check 图标与实线 |
| Color.Node.InProgress | #64D2FF | #0078D4 | 进行中，配对 Play 图标与双线 |
| Color.Node.Pending | #FFD60A | #8A6500 | 待完成，配对 Clock 图标与虚线 |
| Color.Node.Cancelled | #858B96 | #656C76 | 已取消，配对 X 图标与中性点线 |
| Color.Urgency.Critical | #FF453A | #C62828 | 紧急，配对双感叹号与文字 |
| Color.Urgency.High | #FF9F0A | #A65300 | 高，配对向上箭头与文字 |
| Color.Urgency.Normal | #8E8E93 | #6D737C | 普通，中性文字与圆点 |
| Color.Urgency.Low | #858B96 | #656C76 | 低，中性文字与向下箭头；小字号文本满足 4.5:1 |

状态色只用于 2–4px 色条、小图标、文字或描边，不用作整张卡片的高饱和背景。逾期使用 Critical 色并明确显示"已逾期"，不能只把 deadline 变红。

### 2.2 圆角与边框

| Token | 值 | 用途 |
| --- | --- | --- |
| Radius.L2 | 32px | L2 外轮廓 |
| Radius.L3 | 24px | L3 外轮廓 |
| Radius.Collapsed | 999px | L1 提示条胶囊 |
| Radius.Section | 16px | 任务卡片、详情分区 |
| Radius.Control | 12px | 输入框、按钮、菜单 |
| Radius.Chip | 999px | 状态、紧急度、筛选标签 |
| Border.Subtle | 1px | 低对比分隔 |
| Border.Focus | 2px | 键盘焦点环，外偏移 2px |

所有可见矩形面必须使用以上圆角层级之一；嵌套圆角遵循外大内小，避免内层切到外轮廓。

### 2.3 间距

基础间距单位 4px：4 / 8 / 12 / 16 / 20 / 24 / 32。任务卡片内边距 16px，L3 内容分区 24px，岛外边缘安全内距 12px。

### 2.4 字体

- 字体：Segoe UI Variable，回退 Segoe UI。
- Display：28/34 Semibold（L3 标题、空状态）；Title：20/26 Semibold（速览标题）；Card Title：16/22 Semibold（最多两行省略）；Body：14/20 Regular；Caption：12/16 Regular（deadline、紧急度、元数据）。
- 重要倒计时使用 tabular numerals，避免数字变化引起宽度跳动。
- Windows 文本缩放开启后允许内容增高与滚动，禁止缩小字体维持固定高度。

## 3. 三级形态规格

### 3.1 L1 隐藏条

- 视觉条 96×6 逻辑像素；原生窗口可使用 Windows 最小高度，但必须上移到只有底部 4–6px 进入工作区；贴屏顶一侧平直，朝屏幕中心的下侧使用胶囊圆角；
- 主显示器顶部中央；L1 禁用原生 Acrylic，窗口其余区域完全透明，无文字无计数；
- 触发热区约 120×10（顶部中央），用屏幕坐标轮询实现，禁止拦截式透明覆盖窗口。

### 3.2 L2 小面板

- 默认 760×280；宽 560–主屏工作区 80%，高 ≤工作区 45%；外距 12；
- 顶部工具行 52px，只显示新建、展开、更多；排序与设置位于更多菜单；
- 采购凭条 248×176、间距 12；宽度足够时露出下一张边缘；
- 凭条从上到下突出任务名称、下一节点、风险/截止与可交互节点轴；节点轴优先显示上一/当前/下一节点，右上角更多菜单与卡片主体使用分离的真实控件。

### 3.3 L3 大面板

- 最大为主屏工作区 85%，外距 24；圆角 24px；
- 使用 240px 左侧任务栏 + `minmax(0, 1fr)` 当前任务工作区；主内容阅读宽度不超过 960px；
- 任务工作区分为概览、采购节点、资料、提醒、备注，一次渲染一个主分区；
- 节点轨迹横向展开；状态使用显式选择器，线型、图标、文字与颜色共同表达；
- 草稿、归档、设置位于侧栏次级入口；窄屏顶部保留任务选择及草稿、归档、设置入口；设置再分常用、AI 与 Qoder、飞书同步、数据与高级；
- L3 顶栏始终提供带文字的“返回速览”按钮；鼠标离开不改变 L3 层级；
- 所有触控目标 ≥44×44，相邻目标间距 ≥8px。

### 3.4 形态过渡

- 同一窗口连续形变；窗口时长 180–220ms，内容时长 160–200ms；L2/L3 切换不重复启停 Acrylic；
- 只动画 compositor 友好属性（transform、opacity、border-radius）；禁止用高频布局重排模拟过渡；
- L1→L2：内容淡入 + 形变；L2→L3：面板放大 + 分区淡入；反向同理；
- 高对比度或系统"减少动画"开启时直接切换，无过渡。

## 4. 任务卡片

从上到下：

1. 任务标题（最多一行，超出省略号）；
2. 下一个有效节点（使用一次“下一步”短标签，不重复“下一采购动作”）；
3. deadline、逾期与紧急度；普通紧急度不使用强调色；
4. 上一/当前/下一节点轴、四态显式选择与已完成节点数 / 有效节点数；
5. 右上角更多菜单承载任务完成、取消和删除；可访问名称汇总上述信息（读屏器友好）。

杂事卡片：显示"杂事"chip，无进度条与下一节点，其余一致。

## 5. 动效规范

- 展开/收起：窗口插值 180–220ms；卡片进入/删除：位移+缩放+淡入淡出；
- 滑动：跟手位移、松手按速度惯性滚动并吸附最近卡片，阻尼平滑；
- 状态切换：线型、图标与颜色在 160ms 内同步变化；不使用常驻呼吸动画；
- 全部动效可被系统"减少动画"设置一键关闭（回退规则见 3.4）；轮播在该模式下保留直接拖动反馈，松手、滚轮和键盘定位直接吸附，不运行惯性或补间。

## 6. 触控与可访问性

- 触控目标 ≥44×44；支持触摸滑动；长按 450ms 拖拽重排为后续版本；
- 键盘：Tab 焦点顺序为 L2 工具行→卡片；卡片支持方向键、Home/End，Enter/Space 激活；L3 侧栏与工作区顺序稳定；Esc 层层返回；
- 焦点环 2px #64D2FF；状态不只用颜色表达（图标+文字）；
- Dialog 使用 `aria-modal`、焦点锁定并在关闭后恢复焦点；Toast 使用 live region；
- 高对比度或减少动画模式：关闭磨砂和窗口/内容过渡，使用系统颜色；正文对比度满足 WCAG AA。
