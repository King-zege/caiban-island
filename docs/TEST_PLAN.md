# 采办岛 v1 测试计划

## 1. 节点 → 测试机制映射（每个节点完成后执行；全部通过后提交该节点）

| 节点 | 自动化测试 | 手工验收清单 | 通过标准 |
| --- | --- | --- | --- |
| P0 | 文档一致性检查：相对链接可达、标题结构合法、术语与契约跨文档一致、SPEC 覆盖 8 条原始需求 | 人工评审全部文档 | 检查 0 错误；评审无未决冲突 |
| P1 | Vitest 冒烟（工具链与配置） | Win10/Win11：三态切换流畅（60fps 目标）、磨砂生效与纯色回退、悬停不抢焦点、托盘退出正常 | 全部通过 |
| P2 | 单测：排序规则、字段校验、持久化往返 | 触摸/鼠标滑动跟手、吸附正确、数据刷新不跳卡 | 全部通过 |
| P3 | 单测：节点状态流转、链接失效处理、Markdown 安全渲染 | 时间轴编辑与三态切换、链接打开/定位 | 全部通过 |
| P4 | 集成：归档快照格式与恢复往返、提醒调度 | 系统通知禁用时岛内轻弹降级、完成/取消二次确认 | 全部通过 |
| P5 | 集成：本地 mock MCP client 全工具调用、schema 拒绝非法输出 | Qoder 真机联调：注册 SSE、对话生成草稿、审核确认 | 全部通过 |
| P6 | 打包冒烟（zip 解压即用） | Win10×Win11 × 100%/125%/150% DPI × 鼠标/触摸矩阵；高对比度/减少动画回退 | 矩阵全通过 |

## 2. 单元测试清单

- 排序：紧急度→是否有 deadline→deadline 升序→创建时间→ID；逾期表现；空列表。
- 校验：名称 1–200 字符；节点起止时间关系；枚举合法性；UTC/时区换算往返。
- 进度计算：0 节点=尚未拆分；完成数/总数。
- 快照格式：task.json 字段完整性与 format_version；task.md 结构。
- Markdown 安全：原始 HTML/script/iframe 被剔除。
- LLM schema 校验：合法样例通过；缺字段/坏枚举/错顺序拒绝；自动修复一次逻辑。

## 3. 集成测试清单

- SQLite：CRUD、事务回滚、WAL、迁移升级路径、外键级联。
- 归档：完成/取消 → 快照导出 → 归档查询 → 恢复往返；重名快照自动加序号。
- 提醒：创建/修改/取消后与 Windows 计划一致；漏发合并为一条摘要。
- MCP：mock client 经 SSE 调用全部 4 个工具；错误 token 返回 401；非回环来源拒绝；草稿不落正式数据。
- 内置 LLM：mock OpenAI-compatible server；function call 解析；修复一次逻辑；失败草稿可编辑。

## 4. 手工矩阵（真机）

- 系统：Win10 1809、Win10 22H2、Win11 最新（真机或虚拟机）。
- DPI：100%、125%、150%（含混合缩放）。
- 输入：鼠标（悬停/点击/拖拽/滚轮）、触摸（滑动/下滑展开/长按）。
- 场景：浏览器最大化时提示条不遮挡标签；真正全屏视频/游戏时暂停岛；高对比度与减少动画回退；任务栏自动隐藏下定位正确。

## 5. 性能目标

- 展开/收起/滑动动画 60fps（devtools 性能面板核对，无明显掉帧）。
- 冷启动 < 2s；常驻内存 < 250MB（Electron 基线）。
- 100 个活跃任务时列表滚动与排序不卡顿。

## 6. 安全验收

- 全仓扫描：无明文 API Key / MCP token / Authorization（grep + 提交前检查）。
- 日志无请求正文与敏感路径；快照与备份无凭据。
- MCP 端点仅绑定 127.0.0.1；token 重置后旧地址立即失效。

## 7. 打包验收

- 绿色 zip 在干净环境解压双击运行；卸载=删除文件夹。
- 首次运行 SmartScreen 提示与 README 说明一致。
- 开机自启注册/取消正确；卸载不残留多余注册表项。
