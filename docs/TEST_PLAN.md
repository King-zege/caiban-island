# 采办岛 v1 测试计划

## 1. 节点 → 测试机制映射（每个节点完成后执行；全部通过后提交该节点）

| 节点 | 自动化测试 | 手工验收清单 | 通过标准 |
| --- | --- | --- | --- |
| P0 | 文档一致性检查：相对链接可达、标题结构合法、术语与契约跨文档一致、SPEC 覆盖全部需求（成功标准 9 条） | 人工评审全部文档 | 检查 0 错误；评审无未决冲突 |
| P1 | Vitest 冒烟（工具链与配置） | Win10/Win11：三态切换流畅（60fps 目标）、磨砂生效与纯色回退、悬停不抢焦点、托盘退出正常 | 全部通过 |
| P2 | 单测：排序规则、字段校验、持久化往返 | 触摸/鼠标滑动跟手、吸附正确、数据刷新不跳卡 | 全部通过 |
| P3 | 单测：节点状态流转、链接失效处理、Markdown 安全渲染 | 时间轴编辑与显式状态切换、链接打开/定位 | 全部通过 |
| P4 | 集成：归档快照格式与恢复往返、提醒调度 | 系统通知禁用时岛内轻弹降级、完成/取消二次确认 | 全部通过 |
| P5 | 集成：本地 mock MCP client 全工具调用、schema 拒绝非法输出 | Qoder 真机联调：注册 SSE、对话生成草稿、审核确认 | 全部通过 |
| P6 | 集成：mock bitable server（建表/建字段/upsert 幂等、令牌失效重试）、CSV/Markdown 导出格式校验 | 真机联调：个人令牌同步、字段映射正确、自动同步触发、断网重试 | 全部通过 |
| P7 | 打包冒烟（zip 解压即用） | Win10×Win11 × 100%/125%/150% DPI × 鼠标/触摸矩阵；高对比度/减少动画回退 | 矩阵全通过 |
| P8 | 单测：L1 原生最小高度可见区、backdrop 决策、UiPreferences、测试数据目录限制 | 深/浅主题切换；L1 只露 4–6px、无整块 Acrylic、点击穿透 | 全部通过 |
| P9 | 组件测试：L2 键盘浏览、设置直达、L3 分区、显式状态、5 秒撤销；axe 扫描 | 0/1/7/100 任务与长中文；键鼠/触摸完成核心采购流程 | 全部通过；axe 无 serious/critical |
| P10 | 确定性 Electron 截图 + Impeccable detector + 全量 typecheck/test/build/package | Win10/Win11 × 100%/125%/150% DPI × 深/浅/高对比度/减少动画/200% 文本 | 矩阵全通过；机械检测 0 条未处理发现 |
| P11 | 状态机/四态进度/永久删除单测 + L2 节点轴与菜单组件测试 + detector | L1 圆角方向、L2 离开自动收起、L3 返回按钮、窗口切换流畅度 | 全部通过；删除可撤销；机械检测 0 条未处理发现 |
| P12 | 过渡状态机/GPU 分类/缓存/虚拟 Carousel 测试 + 隔离 Electron 性能基准 + detector | 独显/集显/软件渲染 × 三级切换 × 快速反向操作 | 每次切换最多一次 resize；100 任务最多 7 张卡；软件降级功能完整 |
| P13 | 版本常量、MCP 明文迁移/密文损坏/重置、草稿编辑与确认最终校验、AppService 通知测试 + 全量门禁 | 升级含旧明文 token 的隔离数据目录；确认旧地址失效；草稿损坏恢复提示 | SQLite 无明文 token；非法草稿不落正式数据；正式变更提交后通知一次 |
| P14 | Pi faux/mock provider 多轮工具循环、allowlist、轮次/超时/取消、会话恢复、操作提案并发前置条件与 renderer 交互测试 | 使用临时 DeepSeek Key 验证 Flash/Pro、限流/断网/取消；Key 不记录；L1/L2/L3 性能 | 草稿/操作确认前不生效；逐次确认且仅执行一次；会话可删除/导出；package 成功 |
| P15 | 记忆容量/去重/安全扫描、提案审核、FTS5 会话召回与 renderer 无障碍测试 | 审核画像/业务记忆；重载会话验证快照注入；清空与删除 | 未确认内容不注入；敏感内容拒绝；容量不静默淘汰；package 成功 |
| P16 | v4 迁移、节点时间/时区/并发、任务与节点提醒原子领取、生命周期、草稿确认、L2/L3 菜单与对话框测试 + detector + 全量门禁 | L2 设置未来两分钟；Toast 点击定位；通知禁用回退；睡眠恢复摘要；打包 EXE 冒烟 | 单次准时触发；状态/归档正确取消和恢复；漏发不重复；键盘/读屏完整；package 成功 |

## 2. 单元测试清单

- 排序：紧急度→是否有 deadline→deadline 升序→创建时间→ID；逾期表现；空列表。
- 校验：名称 1–200 字符；节点起止时间关系；枚举合法性；UTC/时区换算往返。
- 进度计算：0 有效节点=尚未拆分；已取消节点排除；完成数/有效节点数。
- 快照格式：task.json 字段完整性与 format_version；task.md 结构。
- Markdown 安全：原始 HTML/script/iframe 被剔除。
- LLM schema 校验：合法样例通过；缺字段/坏枚举/错顺序拒绝；自动修复一次逻辑。
- UI 偏好：明暗、高对比度、减少动画与 backdrop 组合；系统变化后状态同步。
- 窗口几何：Windows 最小原生高度下 L1 仍只进入工作区 4–6px；多 DPI 使用逻辑像素。
- 撤销队列：5 秒内撤销不调用正式删除；超时只提交一次；失败恢复可见内容。
- 过渡：阶段顺序、旧 id 忽略、80ms/280ms 超时、preparing 取消、动画中末次请求合并、显示器变化后落到新几何。
- 渲染能力：GPU 状态分类为 composited/software/direct；未知状态保守降级；高对比度、减少动画和 GPU 进程异常强制 direct。
- 虚拟 Carousel：0/1/7/100 项、可见范围与两侧 overscan、全局索引焦点、Home/End、触摸/滚轮后吸附。
- 节点时间：任务时区与 UTC 往返（含 DST 不存在时间）、活跃节点过去时间拒绝、截止早于开始拒绝、超任务 deadline 二次确认、`expectedStartUtc` 并发冲突。

## 3. 集成测试清单

- SQLite：CRUD、事务回滚、WAL、迁移升级路径、外键级联。
- 归档：完成/取消 → 快照导出 → 归档查询 → 恢复往返；重名快照自动加序号。
- 提醒：任务提前量与节点准时提醒并存；只领取实际到期 ID；节点未设时间不调度，时间修改重置、其他字段不重复；完成/取消/删除/归档取消，恢复只重建未来提醒；启动/唤醒漏发合并且重复扫描不重复。
- MCP：mock client 经 SSE 调用全部 4 个工具；错误 token 返回 401；非回环来源拒绝；草稿不落正式数据。
- 内置 LLM：mock OpenAI-compatible server；function call 解析；修复一次逻辑；失败草稿可编辑。
- Pi Agent：Pi faux provider 完成真实多轮工具循环；mock runner 验证单 run、取消、上限、流式映射与会话持久化；不使用真实 Key。
- Pi 打包：构建配置必须内联两个纯 ESM Pi 包；最终 main bundle 不得残留 `require("@earendil-works/pi-*")`，portable 双击启动不得出现 `ERR_PACKAGE_PATH_NOT_EXPORTED`。
- 轻量操作：allowlist/schema、确认前不生效、重复确认、过期/旧值冲突、节点增改删排与提醒提案；删除二次确认和撤销。
- 长期记忆：add/replace/remove 提案确认前不生效；编辑确认、拒绝、直接维护、来源时间、1,375/2,200 字符容量与 80% 警告。
- 记忆安全：规范化重复、不可见 Unicode、提示注入、凭据/Authorization、私人/临时路径和单条长度均在写入前拒绝。
- 会话召回：FTS5 迁移/索引、首尾摘要、有限上下文、tool 消息过滤；会话新建/重载才刷新确认记忆快照。
- 飞书：mock bitable server 建表/建字段/按任务ID upsert 幂等；令牌失效错误与重试；CSV/Markdown 导出格式。
- Renderer：L2 方向键/Home/End/Enter、节点四态与时间的组合菜单、当前态标记、设置/修改/清除、拖动隔离、Esc 焦点恢复；L3 完整起止时间与通知定位；任务更多菜单与 5 秒删除撤销；设置直达，Dialog 焦点锁定和恢复，Toast live region，凭据默认遮罩，外链确认。
- 视觉测试进程必须使用位于系统临时目录的 `CAIBAN_TEST_USER_DATA_DIR` 和合成夹具；禁止读取真实用户数据。
- 软件渲染测试使用隔离目录与 `CAIBAN_TEST_DISABLE_HARDWARE_ACCELERATION=1`；生产包不得接受该变量。

## 4. 手工矩阵（真机）

- 系统：Win10 1809、Win10 22H2、Win11 最新（真机或虚拟机）。
- DPI：100%、125%、150%（含混合缩放）。
- 输入：鼠标（悬停/点击/拖拽/滚轮）、触摸（滑动/下滑展开/长按）。
- 场景：浏览器最大化时提示条不遮挡标签；真正全屏视频/游戏时暂停岛；高对比度与减少动画回退；任务栏自动隐藏下定位正确。
- 内容：0/1/7/100 个任务、长中文、逾期、无节点、草稿、归档、离线失败、凭据遮罩与所有空状态。
- 可访问性：仅键盘完成创建/浏览/编辑/撤销；读屏名称正确；200% 文本缩放无关键内容截断。
- P16 提醒：在 L2 设置未来两分钟的节点时间，核对 Windows Toast 正文与点击定位；禁用系统通知后核对岛内轻弹；睡眠跨过时间后只出现一条漏发摘要。

## 5. 性能目标

- 展开/收起/滑动动画 60fps（devtools 性能面板核对，无明显掉帧）。
- 冷启动 < 2s；常驻内存 < 250MB（Electron 基线）。
- 100 个活跃任务时列表滚动与排序不卡顿。
- 合成模式每次切换最多一次 resize，请求到首个视觉帧 ≤50ms，`animating` 至 `settling` 的视觉过渡 200±40ms；100 任务的 animating 阶段 rAF 间隔不得出现 >50ms、>20ms 间隔占比 <5%，完整切换由 Long Tasks API 验证无 >50ms 主线程长任务。使用 `npm run benchmark:transitions -- <CDP 端口> --assert` 验证；端口对应的 Electron 必须以隔离临时目录和 100 条合成任务启动。
- 普通 L2 在 100 任务时 DOM ≤700、实际 TaskCard ≤7；软件/direct 模式允许降低动效，但启动、返回和全部采购操作必须可用。

## 6. 安全验收

- 全仓与测试数据目录扫描：无明文 API Key / MCP token / PersonalBaseToken / Authorization（grep + 提交前检查）；MCP token 只允许以 safeStorage 密文存在。
- 日志无请求正文与敏感路径；快照与备份无凭据。
- MCP 端点仅绑定 127.0.0.1；token 重置后旧地址立即失效。

## 7. 打包验收

- GitHub 推荐的 Windows x64 独立 EXE 在干净环境双击运行；本地 zip 解压冒烟继续作为打包验收，卸载=删除应用文件。
- 首次运行 SmartScreen 提示与 README 说明一致。
- 开机自启注册/取消正确；卸载不残留多余注册表项。
