---
name: "采办岛"
description: "以采购链路为界面的 Windows 顶部常驻工作台"
---

# 采办岛设计摘要

正式视觉规范位于 `docs/DESIGN_SYSTEM.md`，实际 token 以 `src/shared/designTokens.ts` 为唯一来源。本文件只保留设计工具需要快速载入的方向。

## Creative North Star

“精密采购工作台”：界面像安静、可核对的采购控制面，而不是卡片堆叠的项目后台。采购凭条、合同卡、杂事工作单与节点链路是跨 L2/L3 的视觉签名。

## Hierarchy

- L1：96×6px 的低打扰入口，不显示正文。
- L2：默认按采购项目、合同、杂事排列三条轨道，优先呈现简称、下一动作和风险；Agent 模式只保留对话与输入。
- L3：当前采购项目、合同、杂事或 Agent 的单焦点工作区；合同专用工作区覆盖合同节点、付款开票、验收、资料和备注。
- L2/L3 Agent 共用会话和流式状态，权限、目录、自动化与会话管理只在 L3。

## Visual Language

- 系统 UI 字体、冷黑/自适应浅色表面、单一冰蓝交互强调。
- 状态色只用于小图标、文字、细轨迹或描边，不能铺满卡片，也不能成为唯一线索。
- Acrylic 只用于外壳；内容区主要依靠留白、细分隔和稳定网格。高对比度、减少动画或磨砂失败时回退纯色。
- 圆角与间距只使用共享 token；内容区禁止无业务意义的卡片套卡片、渐变和装饰阴影。

## Interaction and Safety

- 控件最小 44×44px，焦点可见，菜单/对话框关闭后恢复触发点。
- 外链与文件先展示真实目标；凭据默认遮罩。
- Agent 审批卡紧邻相关消息，清楚显示风险与旧值→新值；Bypass 始终有警示。
- 过渡应连续且克制；减少动画时直接定位最终状态。

新增或重做 UI 前只需继续读取 `docs/DESIGN_SYSTEM.md`，无需从本文件复制 token。
