# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

主要用户是 Windows 桌面上的个人采购执行者。他们在浏览器、文档、即时通信与采购资料之间工作，需要在不离开当前任务的情况下快速确认下一步、更新采购节点并保存相关资料。

## Product Purpose

采办岛是常驻桌面顶部的采购任务管理工具。它通过 L1 隐藏入口、L2 快速速览和 L3 深度工作台三个注意力层级，让用户在数秒内找到下一采购动作，并在需要时完成任务、节点、资料、提醒、草稿与归档管理。

成功意味着：入口不遮挡日常工作；L2 能迅速回答“接下来做什么”；L3 能安全、完整地维护正式采购记录；复杂集成不会干扰日常执行。

## Positioning

产品的独特机制是同一个置顶窗口在 L1、L2、L3 之间连续展开，并让采购节点轨迹在速览与深度编辑之间保持上下文，而不是要求用户切换到独立的项目管理后台。

## Operating Context

- 运行于 Windows 10/11，常驻屏幕顶部并通过托盘管理生命周期。
- 用户使用鼠标、触摸与键盘处理多个活跃采购任务。
- 采购资料包括任务字段、节点、链接、Markdown 备注、提醒、AI 草稿、归档快照与单向飞书导出。
- Qoder MCP 与内置模型只生成草稿；正式数据必须经过用户确认。

## Capabilities and Constraints

- renderer 只能通过 preload 白名单 IPC 访问主进程能力。
- 本地 SQLite 是正式数据源；飞书同步只从本地单向导出。
- API Key、MCP token 与 PersonalBaseToken 必须加密保存且不得进入日志、快照或测试数据。
- 界面支持高对比度、减少动画、不同 DPI 与 Windows 文本缩放。
- 本轮重设计保留现有数据模型与业务能力，不新增供应商、金额或审批字段。

## Brand Commitments

- 产品名称为“采办岛”。
- 界面使用简洁、直接的中文采购语言；用户可见文案不暴露内部需求编号或非必要的实现术语。
- 三级注意力模型与采购节点轨迹是必须保留的产品识别特征。

## Evidence on Hand

- 产品、架构、设计与测试契约位于 `docs/`。
- 当前 Electron/React 实现在 `src/`，包含可运行的 L1/L2/L3、任务、草稿、归档、设置、提醒与同步流程。
- 仓库没有可用于宣传的客户、成效指标或第三方背书，后续界面不得编造此类内容。

## Product Principles

1. 下一采购动作优先于功能入口与系统配置。
2. 常驻但不打扰，展开行为可预测且不抢夺焦点。
3. 正式采购记录的修改必须明确、可恢复并提供结果反馈。
4. 日常能力默认可见，高级集成按需展开。
5. 本地数据与用户凭据始终优先保证安全与可控。

## Accessibility & Inclusion

所有核心流程支持键盘与读屏器；交互目标不小于 44×44 逻辑像素；正文和控件满足 WCAG AA；高对比度或减少动画开启时，磨砂与过渡必须可靠降级。
